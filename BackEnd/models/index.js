const Producto = require("./Product");
const Usuario = require("./Usuario");
const Cliente = require("./Cliente");
const Pedido = require("./Pedido");

module.exports = { Producto, Usuario, Cliente, Pedido };
